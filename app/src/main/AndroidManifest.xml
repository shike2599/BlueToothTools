<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.calypso.buetools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApp"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ServerActivity"
            android:label="@string/title_activity_server">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DeviceTypeActivity" />
        <activity android:name=".activity.TongYongCJBActivity" />
        <activity android:name=".activity.DianLiuCJBActivity" />
        <activity android:name=".activity.ZhuanJieBanCJBActivity" />
        <activity android:name=".activity.WangGuanCJBActivity" />
        <activity android:name=".activity.SettingDialog"></activity>
    </application>
    <!-- 添加网络操作权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>